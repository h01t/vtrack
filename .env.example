# Blackbox-native paths for large ML artifacts (do not commit .env).
# Copy to .env and adjust if needed.

VTRACK_DATASETS_DIR=/srv/ai/datasets
VTRACK_CHECKPOINT_DIR=/srv/ai/checkpoints/vtrack
VTRACK_OUTPUT_DIR=/srv/ai/outputs/vtrack

# Optional: absolute path to KITTI yaml (overrides configs/kitti.yaml)
# VTRACK_KITTI_YAML=/home/tron/projects/vtrack/configs/kitti.yaml
